/**
 * Créneaux proposés par l'IA (plus de saisie manuelle).
 * Les créneaux sont générés par analyze-planning Edge Function.
 * Ce composant existe pour compatibilité si des références persistent.
 */
export function PlanningSlotEditor() {
  return null;
}
